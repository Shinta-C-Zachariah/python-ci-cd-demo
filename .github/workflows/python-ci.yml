name: Python CI Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-test:
    runs-on: ubuntu-latest
    
    strategy:
        matrix:
          python-version: ["3.9", "3.10", "3.11"]
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Security scan (Bandit)
      run: bandit -r . --exclude ./test

    - name: Run tests
      run: pytest

  docker-build:
    needs: build-test

    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11"]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Docker image
        run: |
          docker build -t python-app:${{ matrix.python-version }} .

  deploy:
    needs: docker-build

    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11"]

    if: github.ref == 'refs/heads/main'

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}

    - name: Deploy application
      run: |
        echo "Deploying $APP_NAME to $ENVIRONMENT"

    - name: Health check
      run: |
        echo "Checking application health..."

    - name: Send deployment status to Prometheus
      if: github.ref == 'refs/heads/main'
      env:
        PUSHGATEWAY_URL: ${{ secrets.PUSHGATEWAY_URL }}
        SERVICE_NAME: python-app
      run: |
        STATUS=1
        echo "deployment_status{service=\"$SERVICE_NAME\"} $STATUS" | \
        curl --data-binary @- $PUSHGATEWAY_URL/metrics/job/$SERVICE_NAME

